defmodule ExampleElixir do
  @moduledoc """
   A example module used for introducing mix
  """

  @doc """
    Returns the message it is provided
  """
  def publish(message) do
    message
  end
end
